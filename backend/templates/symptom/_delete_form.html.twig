<form method="post" action="{{ path('app_symptom_delete', {'id': symptom.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ symptom.id) }}">
    <button class="btn">Delete</button>
</form>
