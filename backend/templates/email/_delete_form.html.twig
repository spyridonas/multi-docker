<form method="post" action="{{ path('app_email_delete', {'email_id': email.email_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ email.email_id) }}">
    <button class="btn">Delete</button>
</form>
