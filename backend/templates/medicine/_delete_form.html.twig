<form method="post" action="{{ path('app_medicine_delete', {'med_id': medicine.med_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ medicine.med_id) }}">
    <button class="btn">Delete</button>
</form>
