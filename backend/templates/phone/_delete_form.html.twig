<form method="post" action="{{ path('app_phone_delete', {'phone_id': phone.phone_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ phone.phone_id) }}">
    <button class="btn">Delete</button>
</form>
