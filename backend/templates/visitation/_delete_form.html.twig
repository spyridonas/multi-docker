<form method="post" action="{{ path('app_visitation_delete', {'id': visitation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ visitation.id) }}">
    <button class="btn">Delete</button>
</form>
