{% extends 'base.html.twig' %}

{% block title %}Visitation{% endblock %}

{% block body %}
    <h1>Visitation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ visitation.id }}</td>
            </tr>
            <tr>
                <th>Isit_doctor</th>
                <td>{{ visitation.isitDoctor }}</td>
            </tr>
            <tr>
                <th>Visit_department</th>
                <td>{{ visitation.visitDepartment }}</td>
            </tr>
            <tr>
                <th>Visit_date</th>
                <td>{{ visitation.visitDate ? visitation.visitDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Visit_price</th>
                <td>{{ visitation.visitPrice }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_visitation_index') }}">back to list</a>

    <a href="{{ path('app_visitation_edit', {'id': visitation.id}) }}">edit</a>

    {{ include('visitation/_delete_form.html.twig') }}
{% endblock %}
