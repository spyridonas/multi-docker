<form method="post" action="{{ path('app_symptom_history_delete', {'hist_id': symptom_history.hist_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ symptom_history.hist_id) }}">
    <button class="btn">Delete</button>
</form>
